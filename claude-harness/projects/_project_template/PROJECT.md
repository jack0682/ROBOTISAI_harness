# Project: <name>

> Entry point for this project harness. Read this first, then `context.md`,
> `constraints.md`, and any `overrides/`. This template is the slot; fill it per
> project (or use `scripts/new_project.py`).

## One-liner
<What this project is, in a sentence.>

## Workspace
<Where the project's materials live, relative to the harness repo root —
default `../` (the workspace containing this harness repo). Must match
`project.config.yaml` `workspace:`.>

## Status
<active / paused / archived — and the current phase.>

## Goal
<The outcome this project is driving toward.>

## How to work this project
1. Load the universal harness (kernel → protocols → styles).
2. Load required skills (see `project.config.yaml`).
3. Read `context.md`, `constraints.md`, `local_rules.md`, and `overrides/`.
4. Read the latest session/checkpoint before continuing long work.

## Map
- `context.md` — background & current state
- `constraints.md` — hard limits and gates
- `glossary.md` — terms
- `workflows.md` — recurring procedures
- `local_rules.md` — project-only rules
- `memory/` — decisions, open questions, known issues, checkpoints
- `overrides/` — explicit deviations from universal rules

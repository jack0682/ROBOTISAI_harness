---
paths:
  - "**/experiments/**"
  - "**/*.ipynb"
  - "**/train*.py"
  - "**/eval*.py"
  - "**/results/**"
---

# You are touching an experiment — load the experiments scope

Read `claude-harness/scopes/experiments/AGENTS.md` before turning any of this
into a claim. It owns what a result is allowed to support.

Its two named traps, both of which have actually happened here: **construct
independence** — a method may not cite the measure of the thing it enforces by
construction as evidence of its own superiority — and the **circular metric**
anti-pattern it came from. "± 0.000" on one sample is not a statistic.

> This rule fires on the file. `ROUTING.md` Step 2 remains the authority on what
> to load.
